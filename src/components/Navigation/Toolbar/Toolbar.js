import React from 'react';

import './Toolbar.css';

const toolbar = (props) => (
  <div className="Toolbar">
    <div>MENU</div>
    <div>LOGO</div>
    <nav>
      ...
    </nav>
  </div>
);

export default toolbar;